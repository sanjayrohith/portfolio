# Copy to .env.local and fill values

# Public site URL used in metadata, sitemap and robots.txt
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Optional email provider if you wire delivery in /api/contact
# RESEND_API_KEY=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Optional GitHub settings for Projects section
# Public username to fetch repos for (defaults to sanjayrohith if unset)
# NEXT_PUBLIC_GITHUB_USERNAME=
# Token increases GitHub API rate limit (keep it secret, do NOT prefix with NEXT_PUBLIC_)
# GITHUB_TOKEN=
